{
  "buildCommand": "cd wecare && npm ci && npm run build",
  "outputDirectory": "wecare/dist",
  "installCommand": "echo 'Skipping root install, will install in wecare directory'",
  "rewrites": [
    {
      "source": "/_expo/(.*)",
      "destination": "/_expo/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}